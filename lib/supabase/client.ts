// Supabase client removed — this project uses a Node/Mongo backend.
// The original Supabase helpers were here for auth/session handling in a Supabase-based template.
// To avoid bundling @supabase into the frontend, export a minimal stub.

export function createClient() {
  // Return null to indicate no Supabase client is available.
  // Replace calls to this helper with your backend API if needed.
  return null
}
